# Narrative

Deterministic authored dialogue (ADR 0004). `authoredProvider.ts` selects lines
from a committed tree keyed by verdict and event kind. Presentation only — prose
never re-enters game state.
